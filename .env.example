# OpenAI API configuration

# Required: Your OpenAI API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your-api-key-here

# Optional: The OpenAI model to use for financial insights
# Recommended models: gpt-4o, gpt-4o-mini, gpt-3.5-turbo
OPENAI_MODEL_NAME=gpt-4o-mini

# Optional: The OpenAI API base URL (only change if using a different endpoint)
# Default OpenAI API endpoint (do not change unless using Azure or another provider)
OPENAI_BASE_URL=https://api.openai.com/v1
